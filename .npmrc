# Obsidian downloads a release's files from releases/download/<version>/, where
# <version> is the manifest's version exactly. npm tags as `v1.2.3` by default,
# which would put the assets out of reach; this drops the prefix.
tag-version-prefix=
